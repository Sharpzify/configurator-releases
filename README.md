# Configurator releases

Signed release assets for Configurator. This repository contains **no source code** - only
release binaries and their Ed25519 signatures (each asset ships with a `.sig` file).

The application verifies every downloaded asset against a hardcoded public key before
installing it, so this channel can be public without trust issues.
